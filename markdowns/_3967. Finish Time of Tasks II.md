# 3967. [Finish Time of Tasks II](<https://leetcode.com/problems/finish-time-of-tasks-ii>)

*All prompts are owned by LeetCode. To view the prompt, click the title link above.*

*[Back to top](<../README.md>)*

------

> *First completed : June 03, 2026*
>
> *Last updated : June 03, 2026*

------

> **Related Topics** : **N/A**
>
> **Acceptance Rate** : **23.56 %**

------

## Solutions

- [3967-earliest-finish-time-for-land-and-water-rides-ii/solution.java](<../solutions/3967-earliest-finish-time-for-land-and-water-rides-ii/solution.java>)
### Java
#### [solution.java](<../solutions/3967-earliest-finish-time-for-land-and-water-rides-ii/solution.java>)
```Java
class Solution {
    final static int MAX = 300005;

    public int earliestFinishTime(int[] la, int[] lb, int[] wa, int[] wb) {
        int l = MAX, w = MAX, minL = MAX, minW = MAX;
        int n = la.length, m = wa.length;

        for (int i = 0; i < n; ++i)
            l = Math.min(l, la[i] + lb[i]);

        for (int i = 0; i < m; ++i) {
            w = Math.min(w, wa[i] + wb[i]);
            minL = Math.min(minL, Math.max(wa[i], l) + wb[i]);
        }

        for (int i = 0; i < n; ++i)
            minW = Math.min(minW, Math.max(la[i], w) + lb[i]);

        return Math.min(minW, minL);
    }
}

```

