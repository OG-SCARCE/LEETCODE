# 4414. Question 4414

*All prompts are owned by LeetCode. To view the prompt, click the title link above.*

*[Back to top](<../README.md>)*

------

> *First completed : September 12, 2026*
>
> *Last updated : September 12, 2026*

------

> **Related Topics** : **N/A**
>
> **Acceptance Rate** : **Unknown**

------

## Solutions

- [4414-count-values-with-equally-spaced-occurrences-ii/solution.java](<../solutions/4414-count-values-with-equally-spaced-occurrences-ii/solution.java>)
### Java
#### [solution.java](<../solutions/4414-count-values-with-equally-spaced-occurrences-ii/solution.java>)
```Java
class Solution {
    public int countSpecialIntegers(int[] nums) {
        HashMap<Integer, ArrayList<Integer>> map = new HashMap<>();

        for(int i = 0; i < nums.length; i++){
            map.putIfAbsent(nums[i], new ArrayList<>());
            map.get(nums[i]).add(i);
        }
        int ans = 0;

        for(ArrayList<Integer> x : map.values()){
            if(x.size() < 3){
                continue;
            }

            int gap = x.get(1) - x.get(0);

            boolean flag = true;

            for(int i = 2; i < x.size(); i++){
                int currGap = x.get(i) - x.get(i - 1);

                if(currGap != gap){
                    flag = false;
                    break;
                }
            }
            if(flag){
                ans++;
            }
        }
        return ans;
    }
}

```

