# 1447. [Simplified Fractions](<https://leetcode.com/problems/simplified-fractions>)

*All prompts are owned by LeetCode. To view the prompt, click the title link above.*

*[Back to top](<../README.md>)*

------

> *First completed : May 18, 2026*
>
> *Last updated : May 18, 2026*

------

> **Related Topics** : **[Math](<by_topic/Math.md>), [String](<by_topic/String.md>), [Number Theory](<by_topic/Number Theory.md>), [Euclidean Algorithm](<by_topic/Euclidean Algorithm.md>), [Greatest Common Divisor](<by_topic/Greatest Common Divisor.md>)**
>
> **Acceptance Rate** : **70.07 %**

------

## Solutions

- [1447-jump-game-iv/solution.java](<../solutions/1447-jump-game-iv/solution.java>)
### Java
#### [solution.java](<../solutions/1447-jump-game-iv/solution.java>)
```Java
class Solution {
    public int minJumps(int[] nums) {
        int n=nums.length;

        Map<Integer,List<Integer>> map=new HashMap<>();

        for(int i=0;i<n;i++){
            /*int num=nums[i];
            if(!map.containsKey(num)){
                map.put(num,new ArrayList<>());
                map.get(num).add(i);
            }else{
                map.get(num).add(i);
            }*/
            map.computeIfAbsent(nums[i], k -> new ArrayList<>()).add(i);
        }
        Queue<Integer> queue=new ArrayDeque<>();
        boolean[] vis=new boolean[n];

        queue.offer(0);
        vis[0]=true;

        int steps=0;

        while(!queue.isEmpty()){
            int size=queue.size();
            for(int i=0;i<size;i++){
                int idx=queue.poll();
                if(idx==n-1){
                    return steps;
                }
                if(idx-1>=0 && !vis[idx-1]){
                    queue.offer(idx-1);
                    vis[idx-1]=true;
                }
                if(idx+1<n && !vis[idx+1]){
                    queue.offer(idx+1);
                    vis[idx+1]=true;
                }
                if(map.containsKey(nums[idx])){
                    for(int num:map.get(nums[idx])){
                        if(!vis[num]){
                            queue.offer(num);
                            vis[num]=true;
                        }
                    }
                    map.remove(nums[idx]);
                }
            }
            steps++;

        }
        return steps;

        
    }
}

```

